version: "2"
run:
  allow-parallel-runners: true
linters:
  default: none
  enable:
    - copyloopvar
    - dupl
    - errcheck
    - goconst
    - gocyclo
    - govet
    - ineffassign
    - lll
    - misspell
    - nakedret
    - prealloc
    - staticcheck
    - unconvert
    - unparam
    - unused
  exclusions:
    generated: lax
    rules:
      - linters:
          - lll
        path: api/*
      - linters:
          - dupl
          - lll
        path: pkg/*
      # Test files legitimately repeat fixture strings, use long table rows,
      # and take fixed helper arguments — these linters are noise there.
      - linters:
          - goconst
          - lll
          - dupl
          - unparam
          - gocyclo
        path: _test\.go$
    paths:
      - third_party$
      - builtin$
      - examples$
  settings:
    goconst:
      # Ignore protocol/schema vocabulary that recurs by nature — Kubernetes
      # object field keys and JSON-Schema type names. Deduping these into named
      # constants adds no clarity.
      ignore-string-values:
        - "true"
        - "false"
        - "apiVersion"
        - "kind"
        - "metadata"
        - "name"
        - "namespace"
        - "spec"
        - "status"
        - "conditions"
        - "labels"
        - "annotations"
        - "object"
        - "string"
        - "integer"
        - "number"
        - "boolean"
        - "array"
        - "map"
        - "bool"
        - "int"
    staticcheck:
      checks:
        - -QF1008
formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes: [ "github.com/kubernetes-sigs/kro" ]
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
